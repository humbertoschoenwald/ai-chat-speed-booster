name: Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      tag:
        description: "Release tag (example: v1.2.0)"
        required: false
        type: string

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: pnpm/action-setup@v4
        with:
          version: 10

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - run: pnpm i --frozen-lockfile

      - run: pnpm run typecheck

      - run: pnpm run build:all

      - name: Package extensions
        run: |
          rm -f ai-chat-speed-booster-chrome.zip ai-chat-speed-booster-firefox.zip ai-chat-speed-booster-edge.zip ai-chat-speed-booster-safari.zip
          for browser in chrome firefox edge safari; do
            cd "dist/${browser}"
            zip -r "../../ai-chat-speed-booster-${browser}.zip" .
            cd - >/dev/null
          done

      - name: Validate Firefox data collection manifest field
        run: |
          node -e '
            const fs = require("fs");
            const manifest = JSON.parse(fs.readFileSync("dist/firefox/manifest.json", "utf8"));
            const required = manifest?.browser_specific_settings?.gecko?.data_collection_permissions?.required;
            if (!Array.isArray(required) || !required.includes("none")) {
              console.error("Firefox manifest is missing browser_specific_settings.gecko.data_collection_permissions.required=[\"none\"]");
              process.exit(1);
            }
          '

      - name: Validate zip structure
        run: |
          node -e '
            const { execSync } = require("child_process");
            const browsers = ["chrome", "firefox", "edge", "safari"];

            for (const browser of browsers) {
              const zipFile = `ai-chat-speed-booster-${browser}.zip`;
              const raw = execSync(`unzip -Z1 "${zipFile}"`, { encoding: "utf8" });
              const entries = raw
                .split(/\r?\n/)
                .map((line) => line.trim())
                .filter(Boolean)
                .map((line) => line.replace(/^\.\//, ""));

              if (!entries.includes("manifest.json")) {
                console.error(`manifest.json missing at zip root in ${zipFile}`);
                process.exit(1);
              }

              if (entries.some((entry) => entry.startsWith(`${browser}/`))) {
                console.error(`zip has nested ${browser}/ folder in ${zipFile}`);
                process.exit(1);
              }
            }
          '

      - name: Resolve release tag
        id: tag
        run: |
          if [ "${{ github.event_name }}" = "push" ]; then
            echo "value=${GITHUB_REF_NAME}" >> "$GITHUB_OUTPUT"
          elif [ -n "${{ inputs.tag }}" ]; then
            echo "value=${{ inputs.tag }}" >> "$GITHUB_OUTPUT"
          else
            echo "value=v$(node -p 'require("./package.json").version')" >> "$GITHUB_OUTPUT"
          fi

      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.tag.outputs.value }}
          name: ${{ steps.tag.outputs.value }}
          overwrite_files: true
          fail_on_unmatched_files: true
          generate_release_notes: true
          files: |
            ai-chat-speed-booster-chrome.zip
            ai-chat-speed-booster-firefox.zip
            ai-chat-speed-booster-edge.zip
            ai-chat-speed-booster-safari.zip
